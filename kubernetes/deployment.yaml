apiVersion: apps/v1
kind: Deployment
metadata:
  name: clicknbuy
  labels:
    app: clicknbuy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: clicknbuy
  template:
    metadata:
      labels:
        app: clicknbuy
    spec:
      containers:
        - name: clicknbuy
          image: ghcr.io/jenith-datatemplate/java-devsecops-pipeline:sha-a300142cf5dd1f82dd554d7e4ad0e10b6b69ae97
          imagePullPolicy: Always
          ports:
            - containerPort: 5454
          resources:
            limits:
              cpu: "1"
              memory: "768Mi"
            requests:
              cpu: "0.5"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /health
              port: 5454
            initialDelaySeconds: 90
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 5454
            initialDelaySeconds: 60
            periodSeconds: 5
      imagePullSecrets:
        - name: github-container-registry
