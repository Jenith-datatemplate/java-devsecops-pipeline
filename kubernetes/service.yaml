apiVersion: v1
kind: Service
metadata:
  name: clicknbuy
  labels:
    app: clicknbuy
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 5454
      protocol: TCP
      name: http
  selector:
    app: clicknbuy
